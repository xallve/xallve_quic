from setuptools import setup, find_packages

setup(
    name='xallve_quic',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # deps
    ],
    entry_points={
        'console_scripts': [
            # if present
        ],
    },
)
